<form method="post" action="{{ path('app_cafetiere_delete', {'id': cafetiere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cafetiere.id) }}">
    <button class="btn">Delete</button>
</form>
